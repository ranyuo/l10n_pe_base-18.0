<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record id="library_address_view_list" model="ir.ui.view">
            <field name="name">library.address.list</field>
            <field name="model">library.address</field>
            <field name="arch" type="xml">
                <list string="Direcciones">
                    <field name="name"/>
                    <field name="city"/>
                    <field name="country_id"/>
                </list>
            </field>
        </record>

        <record id="library_address_view_form" model="ir.ui.view">
            <field name="name">library.address.form</field>
            <field name="model">library.address</field>
            <field name="arch" type="xml">
                <form string="Dirección">
                    <sheet>
                        <group>
                            <field name="name"/>
                            <field name="street"/>
                            <field name="city"/>
                            <field name="country_id"/>
                        </group>
                    </sheet>
                </form>
            </field>
        </record>

        <record id="library_address_action" model="ir.actions.act_window">
            <field name="name">Direcciones</field>
            <field name="res_model">library.address</field>
            <field name="view_mode">list,form</field>
        </record>

        <menuitem id="library_address_menu"
                  name="Direcciones"
                  parent="library_management.library_menu_root"
                  action="library_address_action"
                  sequence="20"/>

    </data>
</odoo>